export const badPasswordException = {
  passwordError: "Password cannot be empty",
  passwordHash: "Both password and hash must be provided",
};

export const tokenException = {
  invalidToken: "Invalid Token",
};
